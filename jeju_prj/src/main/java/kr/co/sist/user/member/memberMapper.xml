<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.user.member"><!-- namespace: 자바의 패키지와 비슷 -->





	<insert id="insertMember" parameterType="kr.co.sist.user.member.MemberVO">
	   INSERT INTO MEMBER (
            USER_ID,
            PASSWORD,
            USER_NAME,
            PHONE_NUMBER,
            BIRTH,
            GENDER,
            USER_IP,
            USER_STATUS,
            MEMBER_TYPE
        )
        VALUES (
            #{user_id, jdbcType=VARCHAR},
            #{password},
            #{user_name},
            #{phone_number},
            #{birth},
            #{gender},
            #{user_ip},
            #{user_status},
            #{member_type}
        )
	</insert>



	<select id="selectId" resultType="String"
		parameterType="String">
		select USER_ID
		from member
		where  USER_ID  =#{user_id}

	</select>

</mapper>